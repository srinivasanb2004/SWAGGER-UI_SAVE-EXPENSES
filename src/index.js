import React from 'react';
import ReactDOM from 'react-dom/client';
import SaveExpenseForm from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  
    <React.StrictMode>
    <SaveExpenseForm />
  </React.StrictMode>
  
);


